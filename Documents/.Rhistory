panels
gt$heights
panels
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$height[min(panels$t)-1], max(panels$b) + 2)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
gt = gtable_add_grob(gt, stripGrob, t = max(panels$t) + 1, l = min(panels$l), r = max(panels$r))
gt = gt[-(min(panels$r)-1), ]
grid.newpage()
library(grid)
grid.newpage()
grid.draw(gt)
gtable_filter
gt
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
panels
gt
?gtable_add_rows
gt
gt$heights
min(panels$t)
?gtable_add_grob
max(panels$t)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob, t = max(panels$t) + 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[-(min(panels$r)-1), ]
grid.newpage()
grid.draw(gt)
(min(panels$r)
)
gt
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
gt
stripGrob = gtable_filter(gt, "strip-right")
stropGrob
stripGrob
max(panels$t) + 1
min(panels$l)
max(panels$r)
(min(panels$r)-1)
panels
gt
?gtable_filter
gtable_show_layout(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]], t = 0, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(min(panels$r)-1)]
grid.newpage()
grid.draw(gt)
gt = gt[, -(min(panels$r))]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]], t = 0, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(min(panels$r))]
p1
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]], t = 0, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r))]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]], t = 0, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
stripGrob[[1]]
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
stripGrob
attr(stripGrob, "rot")
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.x = element_text(angle = 0, vjust = 1))
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
p1
?theme
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.x = element_text(angle = 90, vjust = 1))
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
p1
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.y = element_text(angle = 90, vjust = 1))
p1
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.y = element_text(angle = 0, vjust = 1))
p1
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 2)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 2)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 2)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
?gtable_add_rows
library(grid)
rect <- rectGrob(gp = gpar(fill = "#00000080"))
tab <- gtable(unit(rep(1, 3), "null"), unit(rep(1, 3), "null"))
tab <- gtable_add_grob(tab, rect, t = 1, l = 1, r = 3)
tab <- gtable_add_grob(tab, rect, t = 1, b = 3, l = 1)
tab <- gtable_add_grob(tab, rect, t = 1, b = 3, l = 3)
dim(tab)
plot(tab)
tab2 <- gtable_add_rows(tab, unit(1, "null"), 1)
dim(tab2)
plot(tab2)
tab3 <- gtable_add_rows(tab, unit(1, "null"))
tab3 <- gtable_add_rows(tab3, unit(1, "null"), 0)
dim(tab3)
plot(tab3)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 0)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gtable_show_layout(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 4)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 5, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.y = element_text(angle = 0))
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 4)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 4, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 3)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 3, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 3)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 2)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt$heights[min(panels$t)]
gt$heights
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[1], 1)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 1, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[1], 2)
#gt <- gtable_add_rows(gt, gt$heights[min(panels$t)-1], 1)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 2, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
gt <- ggplotGrob(p1)
gtable_show_layout(gt)
gt <- gtable_add_rows(gt, gt$heights[1], 3)
gtable_show_layout(gt)
gt$heights[2]
gt$heights[3]
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[3], 3)
gtable_show_layout(gt)
?gtable_add_grob
gt <- gtable_add_rows(gt, gt$heights[3], 6)
gtable_show_layout(gt)
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.y = element_text(angle = 0))
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[3], 3)
gt <- gtable_add_rows(gt, gt$heights[3], 6)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 4, l = min(panels$l), r = max(panels$r))
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 7, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
p1 <- ggplot(inters, aes(time = DSS.time, status = DSS, linetype = Post.Surgical.Treatment)) + geom_km()+ geom_kmticks() +
theme_bw(base_size = 13, base_family = "serif") +
scale_linetype_discrete("Treatment") +
ylab("Disease Specific Survival") + ylim(0, 1) + xlab("Time in years since randomization") +
facet_grid( Set ~ Risk.fac)  + theme(strip.text.y = element_text(angle = 0))
gt <- ggplotGrob(p1)
# Get the positions of the panels in the layout: t = top, l = left, ...
panels <-c(subset(gt$layout, name == "panel", select = t:r))
# Add a row below the x-axis tick mark labels,
# the same height as the strip
gt <- gtable_add_rows(gt, gt$heights[3], 3)
gt <- gtable_add_rows(gt, gt$heights[3], 6)
# Get the strip grob
stripGrob = gtable_filter(gt, "strip-right")
# Insert the strip grob into the new row
gt = gtable_add_grob(gt, stripGrob[[1]][[1]], t = 4, l = min(panels$l), r = max(panels$r))
gt = gtable_add_grob(gt, stripGrob[[1]][[2]], t = 7, l = min(panels$l), r = max(panels$r))
# remove the old strip
gt = gt[, -(max(panels$r) + 1)]
grid.newpage()
grid.draw(gt)
library(xtable)
?xtable
?pander
library(pander)
install.packages('pander')
library(pander)
?pander.data.frame
?pandoc.table
?xtable
install.packages("stargazer")
library(stargazer)
?"stargazer"
confint()
